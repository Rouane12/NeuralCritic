name: Build publication routes (paused)

# Pretty publication routes are temporarily paused while Neural Critic uses the
# proven article.html?slug= runtime. Re-enable only after the generated route
# architecture is rebuilt without affecting article hydration/assets.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Neural Critic pretty-route generation is intentionally paused."
