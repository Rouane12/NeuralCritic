name: Refresh publication discovery files

on:
  push:
    branches: [main]
    paths:
      - scripts/build_sitemap.py
      - assets/supabase-config.js
      - .github/workflows/build-publication.yml
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: neural-critic-publication-discovery-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Refresh sitemap and RSS from published CMS stories
        run: python scripts/build_sitemap.py

      - name: Commit discovery files only when changed
        run: |
          if git diff --quiet -- sitemap.xml feed.xml; then
            echo "Publication discovery files are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sitemap.xml feed.xml
          git commit -m "Refresh Neural Critic discovery files"
          git pull --rebase origin main
          git push
